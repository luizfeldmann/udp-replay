# udp-replay

Tool to replay **UDP** packets from a capture file.

---

# Dependencies

- Dependencies are managed by **conan**:

```sh
conan install .
```

- For debug, pass `-s build_type=Debug --build=missing`

## Build

- Build using **conan**:

```sh
conan build .
```
